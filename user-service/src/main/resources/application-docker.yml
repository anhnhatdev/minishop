server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://postgres-user-db:5432/user_db
    username: postgres
    password: postgres

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka
